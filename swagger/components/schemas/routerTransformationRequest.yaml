title: Router Transformation Request
type: object
properties:
  input:
    type: array
    items:
      type: object
      properties:
        request:
          $ref: './request.yaml'
        message:
          type: object
        metadata:
          $ref: './metadata.yaml'
        destination:
          $ref: './destination.yaml'
  destType:
    type: string

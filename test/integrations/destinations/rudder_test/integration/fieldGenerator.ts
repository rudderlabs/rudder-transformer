import { GenericFieldGenerator } from '../../../common/generators';

// RudderTest uses the generic field generator without customizations
export class FieldGenerator extends GenericFieldGenerator {
  // Inherits all functionality from GenericFieldGenerator
  // No custom behavior needed for rudder_test destination
}

{
  "//": "The secret-paths tooling lives under test/, which the main tsconfig excludes - so it is neither typechecked nor linted by default. It produces a security artifact, so it gets its own typecheck: `npx tsc --noEmit -p tsconfig.secret-paths.json`.",
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": ["./test/secret-paths/**/*.ts", "./src/secretPaths/**/*.ts"],
  "exclude": []
}

/**
 * Masked secrets for rudder_test destination tests
 * This destination doesn't use any secrets, but we include this file
 * to follow the standard pattern for destination tests
 */

// No secrets are needed for this destination
export const secret1 = 'dummy-secret';
export const authHeader1 = 'Bearer dummy-token';
